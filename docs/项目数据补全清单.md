# YOLO26 实验五六项目数据补全清单

补全日期：2026-05-22

本次从 WSL 中已经完成训练的实验目录：

```text
/home/ybh/yubohan_2311311120_exp5_6_yolo26
```

同步回 Windows 项目目录：

```text
C:\Users\Administrator\Desktop\作品集\嵌入式系统课程设计\exp5_6_yolo26
```

## 1. 已补全内容

| 目录 | 内容 | 文件数量 | 说明 |
|---|---:|---:|---|
| `data_raw/images` | VisDrone 原始图片 | 3000 张图片 + README | 真实无人机视角图片 |
| `data_raw/labels` | YOLO 格式标签 | 3000 个标签 + README | 与图片同名的 `.txt` 标注 |
| `my_data/detection/images/train` | 训练集图片 | 2471 | 由脚本划分生成 |
| `my_data/detection/images/val` | 验证集图片 | 677 | 由脚本划分生成 |
| `my_data/detection/labels/train` | 训练集标签 | 2471 | 与训练图片同名 |
| `my_data/detection/labels/val` | 验证集标签 | 677 | 与验证图片同名 |
| `detect_test` | 推理测试图片 | 30 张图片 + README | 用于实验六预测截图 |
| `runs/yolo26` | 训练、验证、预测、导出结果 | 116 | 包含权重、曲线图、预测图 |
| `external/VisDrone` | 原始 VisDrone 数据 | 14040 | 包含 train/val 解压数据和 zip 包 |
| `configs` | 数据集配置 | 3 | 含 `classes.txt`、模板 YAML、Windows YAML |

## 2. 关键模型文件

```text
runs/yolo26/train/weights/best.pt
runs/yolo26/train/weights/last.pt
runs/yolo26/train/weights/best.onnx
```

其中：

```text
best.pt：实验五训练得到的最优 PyTorch 权重
last.pt：最后一次训练保存的权重
best.onnx：实验六导出的 ONNX 模型
```

## 3. 当前类别配置

`configs/classes.txt` 内容应为：

```text
obstacle
```

含义：将 VisDrone 中的 pedestrian、people、bicycle、car、van、truck、tricycle、awning-tricycle、bus、motor 等可碰撞目标统一合并为一类 `obstacle`。

## 4. 数据集配置文件

Windows 当前目录下推荐使用：

```text
configs/my_detect.windows.yaml
```

内容指向：

```text
C:/Users/Administrator/Desktop/作品集/嵌入式系统课程设计/exp5_6_yolo26/my_data/detection
```

如果把项目复制回 Ubuntu/WSL，重新生成本机路径：

```bash
python scripts/02_make_data_yaml.py
```

然后使用：

```text
configs/my_detect.local.yaml
```

## 5. 可直接检查的命令

Windows PowerShell：

```powershell
cd "C:\Users\Administrator\Desktop\作品集\嵌入式系统课程设计\exp5_6_yolo26"
Get-ChildItem .\data_raw\images -File | Measure-Object
Get-ChildItem .\data_raw\labels -File | Measure-Object
Get-ChildItem .\my_data\detection\images\train -File | Measure-Object
Get-ChildItem .\my_data\detection\images\val -File | Measure-Object
Get-ChildItem .\detect_test -File | Measure-Object
Get-ChildItem .\runs\yolo26\train\weights
```

Ubuntu/WSL：

```bash
cd ~/yubohan_2311311120_exp5_6_yolo26
find data_raw/images -maxdepth 1 -type f | wc -l
find data_raw/labels -maxdepth 1 -type f | wc -l
find my_data/detection/images/train -maxdepth 1 -type f | wc -l
find my_data/detection/images/val -maxdepth 1 -type f | wc -l
ls -lh runs/yolo26/train/weights
```

## 6. 说明

当前 Windows 目录已经不再是空壳。它现在包含完整 VisDrone 子集、YOLO 标签、训练验证划分、测试图片、训练结果、验证图、预测图和 ONNX 导出结果。

如果后续继续在 Ubuntu/WSL 中训练，建议以 WSL 路径为准；如果只是整理材料、查看图片、写报告和截图，Windows 目录已经足够完整。
